[project]
name = "grit-realtime"
version = "7.0.0"
description = "GRIT-realtime, a simple fullstack project for the aggregatin and plotting of GRIT ticket data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jira>=3.10.5",
    "maya>=0.6.1",
    "python-dotenv>=1.2.2",
]
