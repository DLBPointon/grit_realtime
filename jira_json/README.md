# JiraJson

Data taken from `https://jira.sanger.ac.uk/rest/api/latest/field`

Inserted into `custom_fields.py` so I didn't have to faff with auth

Parsed by `jira_fields.py` to extract minimal data.
